# This is a basic workflow to help you get started with Actions

name: Crontab CI

on:
  schedule:
    - cron: '1 * * * *'
    
  workflow_dispatch:
  
jobs:
  check-bvt-code:
    strategy: 
      matrix:
        version: [1, 6]
    runs-on: ubuntu-latest
    steps:
      - name: CheckOut
        uses: actions/checkout@v3
      
      - name: echo num
        run: |
          echo ${{ matrix.version }}
        
      - name: Add labels
        uses: actions-cool/issues-helper@v3
        with:
          actions: 'add-labels'
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ matix.version }}
          labels: 'question'
          
      
    
          
     
  
    
